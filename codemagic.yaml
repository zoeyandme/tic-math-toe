workflows:
  android-workflow:
    name: Build Android Debug APK
    environment:
      java: 17
    scripts:
      - name: Build Debug APK
        script: ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
